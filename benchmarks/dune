(executable
  (name perm_count)
  (libraries boxroot ref choice)
  (modules perm_count)
)

(executable
  (name synthetic)
  (libraries boxroot ref)
  (modules synthetic)
)

(executable
  (name globroots)
  (libraries boxroot ref)
  (modules globroots)
)

(executable
  (name local_roots)
  (libraries boxroot)
  (foreign_stubs (language c)
    (names local_roots_stubs)
  )
  (foreign_archives
    ; unclear why this is necessary in addition to (libraries boxroot)
    ; tracked by upstream issue ocaml/dune#4409
    ../boxroot/boxroot)
  (modules local_roots)
)